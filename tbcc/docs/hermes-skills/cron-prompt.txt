Format operator-daily-digest from the fetch JSON already in this prompt. Do not run python, which, Store aliases, or google_api.py. Do not output JSON plans or keystrokes. Rank by consequence. Emit Telegram HTML per the skill Typography section (quote-title cards, emoji outside the quote, primed settings-page links). Never angle-bracket emails, never <u> tags, never markdown > body indents, never 🚨 except same-day money lockout. Never reply [SILENT] — weekends still deliver. Use calendar_analysis and follow_through from the fetch JSON verbatim for overlaps/tentative/open-N-days. Target 2200-3500 chars. If auth_ok is false, TO HANDLE starts with PROBE FAIL — Gmail.
