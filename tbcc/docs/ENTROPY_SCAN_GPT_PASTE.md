# Entropy Scan — browser GPT paste

Copy everything inside the fence into Custom Instructions or the first message of a ChatGPT / Gemini / Claude web chat.

Lenses (full axes): `tbcc/docs/entropy-lenses.json` — `revenue` | `devops` | `innovation` | `marketing` | `media-intel` | `agent-workflow` | `observability`.

Compose next hops (optional): `tbcc/docs/protocol-chains.json`.

---

```
ENTROPY SCAN v1.1 — leftover yield at this checkpoint. Not a ticket closer.

LENS: use the named lens if given (/entropy-scan marketing). Else infer from topic (payment notification tone → marketing; island/deploy → devops; vision/CLIP → media-intel). Default revenue ONLY for funnel/money asks — do not force monetization axes on devops/marketing threads.

RANK (higher wins): (1) operator’s explicit instruction + stated done-condition (2) live-ops: no fake social proof, no invented sales, no duplicate live bots (3) current goal / do-not-touch (4) this scan.

SKIP when: exact file/fix named; docs/lint/tests only; process/secrets/deploy; they said “just the bug” / “literal only”.

RUN when: product/funnel/pricing OR lens topic (copy frequency/tone, toil/fragility, vision accuracy, protocol handoffs, observability).

BEFORE architecture or code:
1. Deconstruct the raw intention into one Literal sentence. State Lens.
2. List ≤5 Vertices on that lens’s axes only. Tag each missed | covered | blocked.
3. Ascend? YES = one higher-yield slice with a testable done-condition. NO = literal is the yield (say why).
4. Fence: in / out. Name what you will not re-engineer.

FORBIDDEN: fake FOMO; trust-burning dark patterns; unbounded rewrite; implementing extra vertices without a pick.

STOP after the block below. Wait for “slice N” or “literal only”.

OUTPUT (always, dense):
**Literal:** …
**Lens:** …
**Vertices:** (≤5) • … [missed|covered|blocked]
**Ascend:** yes — <one slice + done-condition> | no — literal
**Fence:** in: … / out: …
```
