# AOF Hub P9 + P10 — Claude Code handoff

Paste the fenced block below into Claude Code. Reverse report: `tbcc/docs/handoffs/2026-08-10_aof-hub-p9-p10_report.md` then **STOP** for Cursor ACK.

---

## Quota reminder

Run `/usage` in Claude Code before starting. P9 is L-sized; split phases and stop between them.

## Lane note

P9 has product judgment (SEO tube vs owned UGC mix) — if blocked on doctrine, write options in the report and stop. P10 is mechanical dashboard wiring — safe for Lane C.

---

## Handoff block (paste into Claude Code)

```
# AOF Hub P9 + P10 — implementation (Lane C)

## Goal

Ship two independently verifiable slices:

**P9:** Awempire Video Promotion API (VPAPI) + Label4Editor-style label pages on AOF Hub — supplemental SEO tube content that links OUT to Awempire (affiliate), never hosted cams/video on AOF infra.

**P10:** Unified web revenue scoreboard in TBCC dashboard — one panel showing `src_web_*` beacon clicks, touches, and attributed revenue alongside existing gate/bot rows. No Funnel.io.

Done when:
- P9: at least one label route renders real VPAPI-fed videos (or mocked fixture when credentials absent), outbound clicks are beacon-wrapped, disclaimer present.
- P10: dashboard section lists web hub refs (`src_web_*`) with clicks/touches/$ from existing `gate_funnel_report` + `revenue_by_source`.
- Tests pass per verification below; reverse report written.

## Repo roots

- Hub site: `telegram_bot2/aof-forum/` (Next.js 14, App Router)
- TBCC backend + dashboard: `telegram_bot2/tbcc/`

## Read first (in order)

1. `tbcc/docs/handoffs/2026-08-10_aof-hub-frontier-plan-ask_report.md` — P5–P10 context, doctrine
2. `aof-forum/lib/live-embeds.ts`, `aof-forum/data/live-embeds.json` — existing Awempire embed pattern
3. `aof-forum/lib/aof-cta.ts` — web beacon slug env vars
4. `tbcc/backend/app/services/affiliate_beacon_wrap.py`, `tbcc/backend/app/services/click_beacon.py` — L0 beacon wrap
5. `tbcc/backend/app/data/web_hub_beacon_plan.py`, `tbcc/backend/scripts/seed_web_hub_beacons.py` — web CTA slugs (Cursor just added)
6. `tbcc/backend/app/services/gate_funnel.py`, `tbcc/backend/app/services/traffic_attribution.py` — scoreboard data
7. `tbcc/dashboard/src/panels/Analytics.tsx` — existing gate funnel table (extend, don't duplicate)
8. Awempire refs: http://auth.awempire.com/video-promotion-api , https://github.com/DoclerLabs/awe-vpapi-demo , awe-html-kit `data-awe-list="label"` pattern

## Doctrine (non-negotiable)

- Do NOT host cam streams or Awempire video bytes on AOF B2 — VPAPI content is embed/link-out only.
- Do NOT scrape Motherless or mirror third-party tubes at scale.
- Every Awempire outbound URL must be beacon-wrapped (`/r/{slug}`) so `revenue_by_source()` / gate funnel sees `src_web_*` refs.
- Telegram owns conversion — VPAPI pages include `TelegramConversionFooter` (already exists).
- Loot hub strip stays bare (`aof-forum/lib/aof-cta.ts`); contextual pages get attribution.
- No `.env` commits, no secrets in repo.

## P9 scope

### In scope

- `aof-forum/lib/awempire-vpapi.ts` (or similar) — server-side VPAPI client; env:
  - `AWEMPIRE_PSID`, `AWEMPIRE_ACCESS_KEY` (from Awempire dashboard after approval)
  - `AWEMPIRE_VPAPI_BASE` (default per official docs)
- Label catalog config: `aof-forum/data/vpapi-labels.json` — labels to fetch (start with `popular`, operator-expandable)
- Routes:
  - `aof-forum/app/(site)/tube/awempire/[label]/page.tsx` — label landing (grid of cards)
  - Optional: `aof-forum/app/(site)/tube/awempire/watch/[videoId]/page.tsx` — player page linking out
- Components: `VpapiVideoGrid`, `AwempireVpapiDisclaimer` (affiliate disclosure)
- Beacon wrap: use `NEXT_PUBLIC_TBCC_BEACON_BASE` + slugs like `web-vpapi-{label}`; add rows to `web_hub_beacon_plan.py` + seed script
- SEO: `generateMetadata` + `JsonLd` VideoObject on watch pages; add label routes to `sitemap.ts` when labels configured
- Graceful degrade: when credentials missing, show operator setup card (same pattern as `LiveEmbedGrid` unconfigured state)
- Fixture mode for dev: `AWEMPIRE_VPAPI_FIXTURE_JSON` path for tests without live API

### Out of scope

- Replacing owned `media_items` tube feed on `/`
- Live cam iframe work (P5 done in `app/(site)/live/`)
- Island deploy execution
- Awempire account signup (operator)

### P9 verification

```bash
cd aof-forum
npx tsc --noEmit
npm run lint
# optional with fixture:
AWEMPIRE_VPAPI_FIXTURE_JSON=./data/vpapi-fixture.json npm run build
```

Manual: `/tube/awempire/popular` renders grid; outbound click hits `api.powercore.app/r/web-vpapi-popular` (after seed).

## P10 scope

### In scope

- Backend (only if needed): optional `GET /analytics/web-hub-scoreboard?days=30` that filters `gate_funnel_report` rows where `source_ref` starts with `src_web_` — OR filter client-side in dashboard if sufficient.
- Dashboard: new section in `tbcc/dashboard/src/panels/Analytics.tsx` **"Web hub scoreboard"**:
  - Totals: web clicks, touches, attributed $, `attributed_revenue_pct` subset for web refs
  - Table: slug, source_ref, clicks, touches, revenue_usd (reuse gate funnel row shape)
  - Link to `docs/handoffs/2026-08-10_aof-hub-p0-cdn-checklist.md` for operator env
- If new API route: add test in `tbcc/backend/tests/test_gate_funnel.py` or new `test_web_hub_scoreboard.py`
- Ensure `seed_web_hub_beacons.py` slugs documented in panel footer

### Out of scope

- Funnel.io integration
- Awempire commission import API (no official revenue API assumed — clicks/touches only until operator adds manual Awempire CSV later)
- Extension changes

### P10 verification

```bash
cd tbcc/backend
py -3.13 -m pytest tests/test_gate_funnel.py tests/test_income_traffic_source.py -q --tb=no

cd tbcc/dashboard
npm run build
```

## Constraints & gotchas

- `aof-forum` media URLs: stable CDN still operator-blocked (`NEXT_PUBLIC_MEDIA_BASE_URL` empty) — VPAPI thumbs are hotlinked from Awempire, not B2.
- VPAPI rate limits unknown — cache responses server-side (Next `revalidate` or in-memory TTL 15–60 min).
- Dashboard uses `api.analytics.gateFunnel(days)` today — prefer extending before new endpoint.
- Extension version bump NOT needed unless you touch `tbcc/extension/`.
- Windows dev paths use `py -3.13` for backend.

## Working agreement

- Branch: `lane-c/aof-hub-p9-p10` (or work uncommitted — operator merges via Cursor)
- Commit per phase with messages `feat(aof-hub): …` / `feat(dashboard): …`
- Do NOT push unless operator asks
- After **each phase**, write/update `tbcc/docs/handoffs/2026-08-10_aof-hub-p9-p10_report.md` and **STOP** for Cursor ACK

## Phases

### Phase 1 — P9 VPAPI client + label route skeleton

- Add env docs to `aof-forum/.env.example`
- Implement client + fixture mode + `/tube/awempire/[label]` with setup-hint degrade
- Verification: `cd aof-forum && npx tsc --noEmit && npm run lint`
- Write reverse report section "Phase 1"; STOP

### Phase 2 — P9 grid UI + beacon slugs + SEO

- Video grid, watch page, disclaimer, JsonLd, sitemap entries
- Extend `web_hub_beacon_plan.py` + `seed_web_hub_beacons.py` for `web-vpapi-*` slugs
- Verification: same as P9 verification + `py -3.13 scripts/seed_web_hub_beacons.py` dry run
- Write reverse report section "Phase 2"; STOP

### Phase 3 — P10 dashboard scoreboard

- Analytics panel section filtering `src_web_*`
- Optional thin API wrapper if client filter is messy
- Verification: pytest + `npm run build` in dashboard
- Write final reverse report; STOP

## Reverse report structure

# Reverse handoff — AOF Hub P9 + P10
- Status: complete | blocked | needs operator input
- Phase 1/2/3 summary
- Files touched
- Env vars added
- Tests run (paste output)
- Operator steps (Awempire PSID/accessKey, seed beacons on island, paste outbound URLs)
- Blockers
```
