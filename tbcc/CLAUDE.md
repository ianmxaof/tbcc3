# TBCC backend cwd — see repo-root `.claude/CLAUDE.md` for full TBCC/AOF doctrine.

Start Claude Code from `telegram_bot2/` when possible so root settings + CLAUDE.md apply uniformly.

**Cloud-only money:** the **island** (Linux VPS / `api.powercore.app`) is live payment+loot+API. Do not spawn those bots on this PC. Jargon lookup: `.cursor/rules/tbcc-plain-language.mdc`.

**Handoff packing:** messy issue dumps → `.claude/skills/tbcc-directive-generator/SKILL.md` (`/tbcc-directive-generator`). Do not invent Goal/Scope from chat when a pasted directive already defines them.
