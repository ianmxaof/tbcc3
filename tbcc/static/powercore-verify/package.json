{
  "name": "powercore-verify",
  "private": true,
  "description": "Minimal static site for powercore.app (BonusArrive verify + landing)",
  "scripts": {
    "deploy": "wrangler pages deploy . --project-name=powercore-app --branch=main",
    "whoami": "wrangler whoami"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
