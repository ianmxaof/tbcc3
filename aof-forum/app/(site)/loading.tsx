import { MainPaneSkeleton } from "@/components/ShellSkeletons";

/** Instant main-pane swap on in-site navigations (App Router loading UI). */
export default function SiteLoading() {
  return <MainPaneSkeleton />;
}
